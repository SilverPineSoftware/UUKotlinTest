# Consumer ProGuard rules for this library.
# These rules are applied to any consumer of this library when their build
# enables R8/ProGuard. Keep this file minimal — only add rules that callers
# would otherwise have to copy into their own proguard configuration.
